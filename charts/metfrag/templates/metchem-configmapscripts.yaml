apiVersion: v1
kind: ConfigMap
metadata:
  name: configmapscripts
data:
  01-init-metchem.sh: |
    #!/bin/bash
    set -e
    
    echo "Starting metchem database initialization..."
    
    # Download and load prep-metchem SQL dump into postgres database
    echo "Loading prep-metchem data into postgres database..."
    curl --fail --output - https://msbi.ipb-halle.de/~sneumann/prep-metchem-2016.sql.gz | zcat | PGPASSWORD="${POSTGRES_PASSWORD}" psql -U postgres
    
    # Download and load metchem SQL dump into metchem database
    echo "Loading metchem data into metchem database..."
    curl --fail --output - https://msbi.ipb-halle.de/~sneumann/metchem-2016.sql.gz | zcat | PGPASSWORD="${POSTGRES_PASSWORD}" psql -U postgres -d metchem
    
    echo "Database initialization completed successfully."
